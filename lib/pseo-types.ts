// Re-export types only — safe for client components (no fs/path)
export type {
  FundingRatePage,
  ExchangeComparisonPage,
  PseoLearnArticle,
  Exchange,
  Pair,
  Audience,
} from "@/content/pseo/schemas/types"
